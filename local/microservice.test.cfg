inputs:
  lab:
    mssql:
      dsn: "server username password database"
      table:
        event: signal_lab_events
        batch: signal_lab_event_sets
  demographic:
    mongo:
      uri:
      collection:
        event: signal_demographic_events
        batch: signal_demographic_event_sets
  flowsheet:
    mongo:
      uri:
      collection:
        event: signal_flowsheet_events
        batch: signal_flowsheet_event_sets
  mar:
    mongo:
      uri:
      collection:
        event: signal_mar_events
        batch: signal_mar_event_sets
outputs:
  prediction:
    ttl: 120 # 2 * 60 seconds
    mongo:
      uri: "mongodb://mongo/tests"
      collection:
        event: signal_microservice_prediction_events
        batch: signal_microservice_prediction_event_sets
